stages:          # List of stages for jobs, and their order of execution
  - build
  - test
  - deploy
  - package
  - package-test

build-job-ubuntu:
  stage: build
  script:
    - echo "Empty script"
  tags:
    - ubuntu


build-job-centos:
  stage: build
  script:
    - echo "Empty script"
  tags:
    - centos